import 'package:flutter/material.dart';

class SpacerBox extends StatelessWidget {
  const SpacerBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10);
  }
}